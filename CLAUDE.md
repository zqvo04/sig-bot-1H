# CLAUDE.md — sig-bot-1H (WRF-4)

일반 LLM 코딩 실수를 줄이는 행동 지침 + 이 프로젝트의 설계 원칙.

**트레이드오프:** 이 지침은 속도보다 신중함을 우선한다. 사소한 작업은 판단을 사용한다.

---

## 1. 코딩 전에 생각하라

**가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현 전에:
- 가정을 명시적으로 서술한다. 불확실하면 질문한다.
- 해석이 여러 개라면 선택지를 제시한다 — 조용히 하나를 고르지 않는다.
- 더 단순한 접근이 있다면 말한다. 필요하면 반론을 제기한다.
- 무언가 불명확하면 멈춘다. 무엇이 혼란스러운지 지목한다. 질문한다.

## 2. 단순함 우선

**문제를 푸는 최소한의 코드. 투기적 코드 금지.**

- 요청된 것 이상의 기능 추가 금지.
- 단일 사용 코드에 추상화 금지.
- 요청되지 않은 "유연성"이나 "설정가능성" 금지.
- 불가능한 시나리오에 대한 에러 핸들링 금지.
- 200줄로 썼는데 50줄로 가능하다면, 다시 쓴다.

자문하라: "시니어 엔지니어가 과도하게 복잡하다고 할까?" 그렇다면 단순화한다.

## 3. 외과적 변경

**꼭 필요한 것만 건드린다. 자신이 만든 혼란만 정리한다.**

기존 코드를 편집할 때:
- 인접 코드, 주석, 포맷팅을 "개선"하지 않는다.
- 고장나지 않은 것을 리팩터링하지 않는다.
- 다르게 하더라도 기존 스타일을 맞춘다.
- 관련 없는 죽은 코드를 발견하면 언급한다 — 삭제하지 않는다.

자신의 변경이 고아를 만들었을 때:
- 자신의 변경으로 불필요해진 import/변수/함수를 제거한다.
- 기존에 존재하던 죽은 코드는 요청받지 않는 한 제거하지 않는다.

테스트: 변경된 모든 줄은 사용자의 요청으로 직접 연결되어야 한다.

## 4. 목표 주도 실행

**성공 기준을 정의한다. 검증될 때까지 반복한다.**

작업을 검증가능한 목표로 변환한다:
- "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과시킨다"
- "버그 수정" → "재현하는 테스트를 작성하고, 통과시킨다"
- "리팩터링" → "전후 테스트 통과를 확인한다"

여러 단계 작업은 간략한 계획을 제시한다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

---

## 5. 시그봇 설계 원칙

### 5-A. 목적함수

```
max N_signals  s.t.  WinRate ≥ W_floor
```

신호 수를 최대화하되 승률은 플로어 이상을 보장한다.
- 승률은 **보정(calibration)이 보장**한다 — 임계 = 플로어.
- 빈도는 **넓은 유니버스 × 4셋업 × 양방향**의 합집합으로 산다(플로어 불변).
- W_floor를 높이는 것이 아니라 유니버스/셋업 확장으로 빈도를 올린다.

### 5-B. 라이브 봇은 절대 학습하지 않는다

- 학습은 오프라인(`analysis/calibrate.py`)에서만 수행한다.
- 라이브는 `data/calibration_table.json`의 δ_eff만 읽는다.
- 코드에 온라인 학습, 적응형 임계, 동적 가중치 업데이트를 넣지 않는다.

### 5-C. 백분위 상대평가 — 절대 임계 금지

- 지표값의 절대 임계(예: RSI < 30)를 새로 추가하지 않는다.
- 모든 신호는 **코인 자기분포** 백분위(`percentile.py`)로 표현한다.
- 새 피처를 추가할 때는 반드시 롤링 백분위 변환을 거친다.

### 5-D. 롱/숏 완전 대칭

- 모든 디텍터, 피처, 가중치는 롱과 숏에 정확히 대칭이어야 한다.
- 한 방향에만 작동하는 조건, 보너스, 패널티를 추가하지 않는다.
- 비대칭이 발견되면 반드시 양방향으로 수정한다.

### 5-E. 무상태(Stateless)

- 엔진은 `/tmp` 유실과 무관하게 동작해야 한다.
- 상태 파일, 인메모리 캐시, 누적 카운터에 의존하는 로직을 추가하지 않는다.
- 심볼별 JSONL만이 유일한 지속 저장소다.

### 5-F. try/except로 본체 격리

- 심볼 루프의 각 심볼 처리는 `try/except`로 감싼다.
- 단일 심볼 실패가 전체 잡을 중단시켜서는 안 된다.
- 예외는 로그에 기록하고 조용히 계속 진행한다.

### 5-G. WRF-4 엔진 레이어 계층 준수

변경 시 아래 레이어 책임을 침범하지 않는다:

```
L0 VETO    : veto.py — 하드 거부(4종). 여기에 소프트 게이트 추가 금지.
L1 측정    : percentile.py, btc_macro.py, features.py — 순수 측정함수.
             학습 로직, 상태, 부작용 금지.
L2 디텍터  : detectors.py — C/L/F 축 산출 + precond 게이트.
             4셋업 밖의 새 셋업은 Phase 2 보정 검증 후에만 추가.
L3 보정    : calibration.py — δ_eff 소비 + prior 폴백. 학습 안 함.
L4 발사    : engine.py — P̂ ≥ floor ∧ ¬VETO → 발사판정. 점수 합산 로직 금지.
```

### 5-H. 셀 키 차원을 늘리지 않는다

- 보정 셀 키는 `(setup × regime_1h × btc_macro)` 고정.
- 새 맥락(4H추세, 일봉EMA 등)은 셀 키 확장이 아니라 **C축 연속피처**로 주입한다.
- 셀 키 차원 추가는 콜드스타트 재시작 + 수개월 데이터 요구 — 사전 승인 없이 금지.

### 5-I. 검증 → 점등 원칙

- 새 기능은 기본 OFF 토글(`WRF_*` 환경변수)로 추가하고 되돌리기 가능하게 한다.
- 데이터 없이 "논리적으로 맞으니 켠다"를 하지 않는다.
- 단계별 진입조건(Gate-In)과 졸업조건(Gate-Out)이 충족된 후에만 다음 단계를 켠다.

### 5-J. 합산 점수제 패턴 금지

레거시(v4/v5)의 실수를 반복하지 않는다:
- 보너스/패널티의 합산 누적으로 임계를 조정하는 패턴을 추가하지 않는다.
- `SCORE_WEIGHTS`, `BONUS_*`, `PENALTY_*` 스타일 파라미터를 WRF 엔진에 추가하지 않는다.
- 판정은 오직 `P̂ ≥ W_floor`로 한다. 점수로 "자기 자신과 싸우는 기계"를 만들지 않는다.

### 5-K. 파라미터 추가 원칙

새 파라미터는 아래 조건을 모두 충족해야 한다:
1. `config.py`의 `WRF_*` 네임스페이스에 추가한다.
2. 환경변수로 오버라이드 가능하게 한다(`_wrf_f` / `_wrf_i` 헬퍼 사용).
3. 기본값은 기존 동작을 보존하는 값(되돌리기 가능)으로 설정한다.
4. README의 해당 섹션에 문서화한다.

실질 튜닝 파라미터는 **3~5개** 이내로 유지한다. 단일변수·워크포워드로만 튜닝한다.

---

**이 지침이 작동하는 기준:** 불필요한 변경이 diff에서 줄고, 과도한 복잡성으로 인한 재작성이 줄고, 구현 후가 아닌 전에 명확화 질문이 나온다.
